# A2.1.3 — Portfolio Risk Architecture Validator

A2.1.3 is the entry gate for A2.2.

The validator persists evidence in `Portfolio Risk Validation` and separates blocking architecture defects from non-blocking operational observations.

A2.2 entry criterion: `failedControls = 0`.
