# A2.2.1 Numeric Stability Fix

The first A2.2 aggregation allowed non-finite values to propagate into Google Sheets, producing `#NUM!` and false LOW/PASS classifications. This fix normalizes numeric inputs, recalculates weights, validates every aggregate input, and stops execution on invalid data.
